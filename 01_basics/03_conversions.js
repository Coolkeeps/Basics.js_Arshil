let score = "3312ca"
console.log(typeof score);

ScoreInString = Number(score)// the string is converted to no but while printing its NAN
console.log(typeof ScoreInString);
console.log(ScoreInString); 

// if boolean value is chosen and its true then =>1 else =>0
// if null is chosen then it returns =>0
// if undefined is chosen it returns => NAN
// empty string returns false





